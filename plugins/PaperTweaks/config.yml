enable-bstats: true
# Settings related to the embedded database. Don't change these, they are just there if you want to look inside the H2 database for yourself
database:
  # Currently supported: H2, SQLITE. Changing the type will NOT transfer the data.
  type: "H2"
  user: "user"
  password: "password"
